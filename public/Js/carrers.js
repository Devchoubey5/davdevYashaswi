const { text } = require("express");

function CheckValidation()
if (isvalidate){
    if (jquery('#Name').val() == ' '){
        getvalidate('Name', 'text', 'candidate name');
        return false;
    }
    if (jquery('#Mobile-no').val() == ' '){
        getvalidate('Mobile-no', 'number', 'candidate name');
        return false;
    }
    if (jquery('#Message').val() == ' '){
        getvalidate('Message', 'text', 'textarea');
        return false;
    }
    if (jquery('#Resume-upload').val() == ' '){
        getvalidate('Resume-upload', 'Document', 'Upload');
        return false;
    }
    else{

        Insupdcarrer();
    }

}

function Insupdcarrer(){
    var MasterData ={
        p
    }
}